package Compulsory;

/**
 * Comparable is an interface that defines a method copareTo that compares two objects
 *
 * @author Delia
 */
public interface Comparable {
    int compareTo(Object o);
}